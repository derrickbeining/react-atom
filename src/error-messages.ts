/** @ignore */
export const calledUseAtomOutsideFunctionComponent =
  "useAtom can only be called inside the body of a function component";

/** @ignore */
export const calledUseAtomWithNonAtom =
  "useAtom only accepts `react-atom` Atoms, but got:";

/** @ignore */
export const calledDerefWithNonAtom =
  "deref only accepts `react-atom` Atoms, but got:";
